import React from 'react'
import DisplayInfo from '../Components/NotFound/DisplayInfo'

const NotFound: React.FC = () => {
  return (
    <main>
        <DisplayInfo/>
    </main>
  )
}

export default NotFound